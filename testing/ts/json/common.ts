// generated by Textmapper; DO NOT EDIT

import * as token from './token';

export const debugSyntax = false;

export function debugLog(...data: any[]) : void {
  // No-op. DebugParser == false
}

export class Symbol {
  symbol: token.TokenType;
  offset: number;
  endoffset: number;

  constructor(symbol: token.TokenType, offset: number, endoffset: number) {
    this.symbol = symbol;
    this.offset = offset;
    this.endoffset = endoffset;
  }

  copy() : Symbol {
    let copy = JSON.parse(JSON.stringify(this));
    return copy as Symbol;
  }
}

export type StackEntry = {
  sym: Symbol;
  state: number;
};

