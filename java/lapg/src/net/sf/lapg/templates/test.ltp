
# default template
#


${template my2-}

${this[0].name.toUpperCase()}

${if this.length == 1 && ('j\'o' || false)} ${this.length} ${end}

${foreach a in this[0]-}
${index} : ${a.name}
${end}

${end} 

${template qpm-}
${end} 
