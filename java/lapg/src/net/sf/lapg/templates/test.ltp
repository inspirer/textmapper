
# default template
#


${template my2-}

${this[0].name.toUpperCase()}

${if this.length == 1 && ('j\'o' || false)} ${this.length} ${end}

${foreach a in this-}
${index} : ${a.name}
${end}

${call id(1) for this[0]}

${end} 

${template qpm(a,b)-}
${end} 
