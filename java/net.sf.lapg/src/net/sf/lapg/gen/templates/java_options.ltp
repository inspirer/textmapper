${template options-}
group "general" {
	[title=""]
	prefix : string;

	[title=""]
	package : string;

	[title=""; radio=4]
	positions : set(line,column,offset);

	[title=""; check]
	breaks : boolean(on,off);

	[title=""; check]
	lexemend : boolean(on,off);
	
	[title=""; check]
	gentree : boolean(on,off);
}

${end}
